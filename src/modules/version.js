// Single source of truth for app versioning.
// BUMP APP_VERSION BY 0.0.1 ON EVERY CHANGE (e.g. 1.0.1 -> 1.0.2).
// BUMP SW_BUILD ON EVERY DEPLOY to force PWA update detection.
export const APP_VERSION = '1.0.1';
export const SW_BUILD = 11;
