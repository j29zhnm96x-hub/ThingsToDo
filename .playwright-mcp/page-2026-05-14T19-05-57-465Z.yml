- generic [ref=e2]: "import { el, clear, emptyState } from '../ui/dom.js'; import { renderTodoList } from '../ui/todoList.js'; import { pickProject } from '../ui/pickProject.js'; import { confirm } from '../ui/confirm.js'; import { moveTodo, reorderBucket, completeTodo, uncompleteTodo, getAllTodosForProject } from '../logic/todoOps.js'; import { compressAttachmentsForArchive } from '../logic/attachments.js'; import { openTodoMenu } from '../ui/todoMenu.js'; import { openTodoInfo } from '../ui/todoInfo.js'; import { renderProjectCard } from '../ui/projectCard.js'; import { openProjectMenu } from '../ui/projectMenu.js'; import { hapticLight } from '../ui/haptic.js'; import { Priority } from '../data/models.js'; import { t } from '../utils/i18n.js'; import { renderVoiceMemoList, openPlaybackModal, openVoiceMemoMenu } from '../ui/voiceMemo.js'; import { openModal } from '../ui/modal.js'; import { isDueNowOrPast } from '../logic/recurrence.js'; import { renderCalendarGrid, buildCalendarData } from '../ui/calendarView.js'; const PRIORITY_ORDER = ['URGENT', 'P0', 'P1', 'P2', 'P3']; let collapsedPriorities = {}; async function buildProjectsById(db) { const projects = await db.projects.list(); const map = new Map(projects.map((p) => [p.id, p])); return { projects, map }; } export async function renderInbox(ctx) { const { main, db, modalHost } = ctx; clear(main); // View toggle: All / Today / Calendar const view = sessionStorage.getItem('inboxView') || 'all'; const calYear = parseInt(sessionStorage.getItem('calYear') || String(new Date().getFullYear()), 10); const calMonth = parseInt(sessionStorage.getItem('calMonth') || String(new Date().getMonth()), 10); const calDay = parseInt(sessionStorage.getItem('calDay') || '0', 10); const toggleAll = el('button', { class: `pill-btn${view === 'all' ? ' pill-btn--active' : ''}`, type: 'button', onClick: () => { sessionStorage.setItem('inboxView', 'all'); renderInbox(ctx); } }, t('allTasks')); const toggleToday = el('button', { class: `pill-btn${view === 'today' ? ' pill-btn--active' : ''}`, type: 'button', onClick: () => { sessionStorage.setItem('inboxView', 'today'); sessionStorage.removeItem('calDay'); renderInbox(ctx); } }, t('today')); const toggleCal = el('button', { class: `pill-btn${view === 'cal' ? ' pill-btn--active' : ''}`, type: 'button', onClick: () => { sessionStorage.setItem('inboxView', 'cal'); renderInbox(ctx); } }, 'ðŸ“…'); const viewToggle = el('div', { class: 'view-toggle', style: 'display:flex;gap:6px;margin-bottom:12px' }, toggleAll, toggleToday, toggleCal); const allTodos = await db.todos.listActive(); let filteredTodos = allTodos; let selectedDay = 0; let calSection = null; if (view === 'today') { const todayKey = new Date().toISOString().slice(0, 10); filteredTodos = allTodos.filter(t => { if (!t.dueDate) return false; const dueKey = t.dueDate.slice(0, 10); return dueKey <= todayKey; }); } else if (view === 'cal') { // Build calendar data from ALL active todos (not filtered by inbox visibility) const { countMap, todoMap } = buildCalendarData(allTodos); selectedDay = calDay; const handleNav = (dir) => { let y = calYear, m = calMonth; if (dir === 'prev') { m--; if (m < 0) { m = 11; y--; } } if (dir === 'next') { m++; if (m > 11) { m = 0; y++; } } sessionStorage.setItem('calYear', String(y)); sessionStorage.setItem('calMonth', String(m)); renderInbox(ctx); }; const handleDay = (day) => { sessionStorage.setItem('calDay', String(day)); renderInbox(ctx); }; calSection = el('div', { style: 'margin-bottom:12px' }, renderCalendarGrid(calYear, calMonth, countMap, selectedDay, (val) => { if (val === 'prev' || val === 'next') handleNav(val); else handleDay(val); }) ); // Filter todos to only show selected day's tasks if (selectedDay > 0) { const dateStr = `${calYear}-${String(calMonth+1).padStart(2,'0')}-${String(selectedDay).padStart(2,'0')}`; filteredTodos = todoMap.get(dateStr) || []; } else { filteredTodos = []; } } // Filter for inbox items and exclude future recurring instances const todos = filteredTodos.filter(t => { // Include if it's inbox or linked to inbox if (!(t.projectId === null || t.showInInbox === true)) { // Check if it's a task with due date and inboxBefore setting if (t.dueDate && t.projectId !== null) { // inboxBefore: null = never, 0 = on due date, N = N days before // undefined (old data) = treat as 0 (on due date) const daysBefore = t.inboxBefore === undefined ? 0 : t.inboxBefore; if (daysBefore === null) { return false; // User picked \"Never\" } const dueTime = new Date(t.dueDate).getTime(); const now = Date.now(); const msBefore = daysBefore * 24 * 60 * 60 * 1000; if (now >= dueTime - msBefore) { t.isVirtualLink = true; } else { return false; } } else { return false; } } // Exclude future recurring instances (not yet due) if (t.isRecurringInstance && t.dueDate && !isDueNowOrPast(t.dueDate)) return false; return true; }); const { projects, map: projectsById } = await buildProjectsById(db); // Projects linked to Inbox const linkedProjects = projects.filter((p) => p.showInInbox); const linkedProjectStats = new Map(); for (const p of linkedProjects) { const allTodos = await getAllTodosForProject(p.id, db, projectsById); const nonArchived = allTodos .filter((t) => !t.archived) .filter((t) => { // Exclude future recurring instances if (t.isRecurringInstance && t.dueDate && !isDueNowOrPast(t.dueDate)) return false; return true; }); const total = nonArchived.length; const completed = nonArchived.filter((t) => t.completed).length; const active = total - completed; linkedProjectStats.set(p.id, { total, completed, active }); } // Voice memos in inbox const voiceMemos = await db.voiceMemos.listForInbox(); const settings = await db.settings.get(); const groupActiveTasks = settings.groupActiveTasks === true; const activeTodos = todos.filter(t => !t.completed); const list = renderTodoList({ todos, projectsById, mode: 'active', onTap: (todo) => { // If linked card (has project but shown here), navigate to project if (todo.projectId && (todo.showInInbox || todo.isVirtualLink)) { location.hash = '#project/' + todo.projectId; return; } openTodoInfo({ todo, db, modalHost, onEdit: () => ctx.openTodoEditor({ mode: 'edit', todoId: todo.id, projectId: todo.projectId, db }) }); }, onToggleCompleted: async (todo, checked) => { if (checked) { // Use completeTodo for proper recurring task handling. // Override to P3 (Low) on completion so completed tasks archive with low priority. await completeTodo(db, { ...todo, priority: Priority.P3 }); } else { await uncompleteTodo(db, todo); } await renderInbox(ctx); }, onEdit: (todo) => { if (todo.isVirtualLink) return; // No edit for virtual links ctx.openTodoEditor({ mode: 'edit', todoId: todo.id, projectId: todo.projectId, db }); }, onMove: async (todo) => { const dest = await pickProject(modalHost, { title: 'Move toâ€¦', projects, includeInbox: true, initial: todo.projectId ?? null, confirmLabel: 'Move' }); if (dest === undefined) return; await moveTodo(db, todo, dest); await renderInbox(ctx); }, onLinkToggle: async (todo) => { // Toggle inbox link await db.todos.put({ ...todo, showInInbox: !todo.showInInbox }); await renderInbox(ctx); }, onArchive: async (todo) => { if (todo.protected) { openModal(modalHost, { title: 'Task Protected', content: el('div', {}, 'This task is protected. Please uncheck \"Protect task\" in the editor to archive it.'), actions: [{ label: t('ok'), class: 'btn btn--primary', onClick: () => true }] }); return; } const ok = await confirm(modalHost, { title: 'Archive todo?', message: 'You can restore it later from Archive.', confirmLabel: 'Archive' }); if (!ok) return; await db.todos.put({ ...todo, archived: true, archivedAt: new Date().toISOString(), archivedFromProjectId: todo.projectId, priority: Priority.P3 }); await compressAttachmentsForArchive(db, todo.id); await renderInbox(ctx); }, onMenu: (todo, { onLinkToggle } = {}) => openTodoMenu(modalHost, { title: todo.title || 'Todo', actions: [ { label: t('edit'), class: 'btn', onClick: () => (ctx.openTodoEditor({ mode: 'edit', todoId: todo.id, projectId: todo.projectId, db }), true) }, // Only show Unlink for Project tasks that are here via link ...(todo.projectId && todo.showInInbox ? [{ label: 'Unlink from Inbox', class: 'btn', onClick: async () => { if (onLinkToggle) await onLinkToggle(todo); else { await db.todos.put({ ...todo, showInInbox: !todo.showInInbox }); await renderInbox(ctx); } return true; } }] : []), { label: t('share'), class: 'btn', onClick: async () => { const { exportTodoToFile } = await import('../utils/share.js'); await exportTodoToFile(db, todo); return true; } }, { label: t('move'), class: 'btn', onClick: async () => { const dest = await pickProject(modalHost, { title: t('moveToProject'), projects, includeInbox: true, initial: todo.projectId ?? null, confirmLabel: t('move') }); if (dest === undefined) return false; await moveTodo(db, todo, dest); await renderInbox(ctx); return true; } }, { label: t('archiveItem'), class: 'btn btn--danger', onClick: async () => { if (todo.protected) { openModal(modalHost, { title: t('taskProtected'), content: el('div', {}, t('taskProtectedMsg')), actions: [{ label: t('ok'), class: 'btn btn--primary', onClick: () => true }] }); return true; } const ok = await confirm(modalHost, { title: t('archiveTodo'), message: t('archiveTodoMsg'), confirmLabel: t('archiveItem') }); if (!ok) return false; await db.todos.put({ ...todo, archived: true, archivedAt: new Date().toISOString(), archivedFromProjectId: todo.projectId }); await compressAttachmentsForArchive(db, todo.id); await renderInbox(ctx); return true; } } ] }), onReorder: async ({ priority, projectId, orderedIds }) => { await reorderBucket(db, { priority, projectId, orderedIds }); await renderInbox(ctx); }, groupByPriority: groupActiveTasks, collapsedPriorities, onGroupToggle: (priority) => { collapsedPriorities[priority] = !collapsedPriorities[priority]; renderInbox(ctx); } }); const linkedProjectsList = linkedProjects.length ? el('div', { class: 'list' }, ...linkedProjects.map((p) => renderProjectCard({ project: p, stats: linkedProjectStats.get(p.id) || { total: 0, completed: 0, active: 0 }, onOpen: () => { hapticLight(); location.hash = `#project/${p.id}`; }, onMenu: () => { hapticLight(); openProjectMenu(modalHost, { db, project: p, onChange: () => renderInbox(ctx) }); } })) ) : null; // Voice memos section const voiceMemosList = voiceMemos.length ? el('div', { style: 'margin-top: 16px;' }, el('div', { style: 'font-size: 0.8125rem; font-weight: 600; color: var(--muted); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 12px; padding: 0 4px;' }, t('voiceMemos')), renderVoiceMemoList({ memos: voiceMemos, modalHost, db, projects, onChange: () => renderInbox(ctx) }) ) : null; if (todos.length === 0 && linkedProjects.length === 0 && voiceMemos.length === 0) { const emptyWrapper = el('div', { style: 'display:flex;flex-direction:column;gap:8px' }, viewToggle, emptyState(t('noTasks'), t('noTasksHint'))); main.append(emptyWrapper); } else { const content = el('div', { class: 'stack' }); content.append(viewToggle); if (calSection) content.append(calSection); if (linkedProjectsList) content.append(linkedProjectsList); if (voiceMemosList) content.append(voiceMemosList); if (todos.length) { if (groupActiveTasks && activeTodos.length > 0) { // Only priorities with 2+ tasks get a collapsible group header const collapsiblePriorities = PRIORITY_ORDER.filter(p => activeTodos.filter(t => t.priority === p).length >= 2 ); if (collapsiblePriorities.length > 0) { const allCollapsed = collapsiblePriorities.every(p => collapsedPriorities[p] === true); const collapseBtn = el('button', { type: 'button', class: 'inbox-collapse-btn', onClick: () => { if (allCollapsed) { collapsiblePriorities.forEach(p => { delete collapsedPriorities[p]; }); } else { collapsiblePriorities.forEach(p => { collapsedPriorities[p] = true; }); } renderInbox(ctx); } }, allCollapsed ? t('expandAll') : t('collapseAll')); content.append(el('div', { class: 'inbox-collapse-bar' }, collapseBtn)); } } content.append(list); } main.append(content); } }"